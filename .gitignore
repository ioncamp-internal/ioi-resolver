.DS_Store

# Generated contest data (export output, not source)
contest.json
contest.manifest.json

# Raw 24MP camera originals (~530 MB). The award slides under slides/ are the
# downscaled copies of the six that get shown -- those stay tracked.
IONC_photo/

# Timestamped dumps from resolver-export (submissions.csv, tasks.json, manifests).
# The tool itself under resolver-export/ stays tracked -- only its output is ignored.
resolver-export/exports/

__pycache__/
*.pyc
